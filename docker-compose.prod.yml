services:
  backend:
    command: gunicorn --bind 0.0.0.0:8000 bookmyfilm.wsgi:application

  frontend:
    container_name: frontend
    build: ./frontend

  nginx:
    volumes:
      - ./nginx/bookmyfilm.prod.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - frontend
      - backend